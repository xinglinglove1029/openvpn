# 2026-08-09

- `/bmad-dev-auto` 对 AI 聊天回复中 `data:` 前缀重复问题的自动诊断已因工作树非干净而按流程阻止；当前未提交改动直接涉及前后端 AI 流式聊天实现。诊断结果记录在 `_bmad-output/implementation-artifacts/bmad-dev-auto-result-ai-chat-stream-diagnosis.md`。
- 已完成并提交 AI SSE 解析一致性修复（`13b51be fix(ai): unify SSE response parsing`）：新增 `frontend/src/lib/sse.ts`，独立 AI 页面与悬浮 AIWidget 统一使用该解析器；支持多 data 行、LF/CRLF、无空格前缀、UTF-8/事件跨分块和 EOF 尾事件。Playwright 4 项协议回归测试和 Go AI 包测试通过。
- 排查部署后 Ollama 不可用：存在三层独立问题：Ollama 进程在 supervisor 中反复崩溃（FATAL 是结果，需看其首次 stderr）；配置曾将 DeepSeek endpoint/provider 与 `qwen2.5:7b` 混用而被 API 400 拒绝；Ollama 仅监听 IPv4 `127.0.0.1:11434`，但 UI 配置 `localhost` 会被 Go 优先解析为 IPv6 `::1`，导致 connection refused。前端切换 provider 仅自动切 BaseURL、不会同步模型名称，容易重现混配。
- 已用本地管理员账号在 `http://127.0.0.1:5173` 完成悬浮 AI 助理端到端实测：DeepSeek 流式回复正常完成，未出现 `data:` 协议前缀，输入重新可用；测试回复内容在视觉上被 Markdown 折叠为同一段，是模型返回内容未带明确 Markdown 硬换行导致，不是 SSE 解析问题。截图保存为 `outputs/ai-stream-live-test.png`。
- 提交 `ba4a45e fix(ai): 修复健康缓存/流式panic/Ollama运行时/扩展Agent工具`，一次性解决 4 类问题：(1) 健康检查 `?refresh=true` 强制探测+热切换后立即 `CheckOnce` 刷新缓存；(2) 流式 `iter.Seq` 所有 yield 检查返回值，消费者 break 时立即停止生产，修复 "range function continued iteration" panic；(3) Dockerfile 运行时从 Alpine 改为 Debian bookworm-slim（glibc），解决官方 Ollama `fcntl64: symbol not found` 崩溃；`localhost`→`127.0.0.1` 避免 IPv6 连接拒绝；(4) 新增 `get_system_counts` Agent 工具查询用户/客户端统计，`AIToolService` 注入 `*ovpn` 获取在线客户端数。
- 端到端 AI Agent 工具实测已全部通过：(a) "列出当前所有用户，查看账号数量与状态" → AI 调用 list_users + get_system_counts，返回完整用户表 + 用户总数 3/启用 3/客户端配置 2/在线 0/managementOk=false；(b) "帮我创建 aaaa 用户"（无邮箱）→ AI 主动询问缺少字段；(c) "创建 aaaa，登录账号 aaaa，邮箱 aaaa@example.com" → AI 调用 create_user（SSE 200，post /ovpn/ai/chat 666ms）。后端日志显示 0 个 Chat handler panic，4 个 chat 请求均 HTTP 200。
- 教训：本地 dev 后端进程（46228）跑的是 `C:/Users/.../go-build*/exe/openvpn-web.exe`（临时目录的旧 build），重启 dev 服务后才能加载新二进制；后续任何 `go build` 后必须显式 kill+重启服务，否则前端复测的还是旧代码导致同样的 "内部错误，请重试" 误判。Docker 容器部署后只要 rebuild 镜像重启容器即可。
