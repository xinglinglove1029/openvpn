# Changelog

All notable changes to this project will be documented in this file.

## [v0.1.15] - 2026-07-27

### Bug Fixes

- **MFA 绑定逻辑不一致修复**
  - 修复新建用户时勾选 MFA 选项导致的逻辑不一致问题（`MfaEnabled=true` 但 `MfaSecret` 为空）
  - 移除新建用户表单中的 MFA 选项，MFA 只能由用户登录后在个人设置中自助绑定
  - 统一 MFA 判断逻辑：`MfaEnabled=true || MfaSecret!=""`，确保向后兼容历史数据

- **客户端配置生成地址错误**
  - 修复 Docker 容器环境下生成的客户端配置 `remote` 地址为容器内部 IP（如 127.0.0.1）的问题
  - 改用 Go 原生配置生成逻辑，从多来源按优先级获取公网 IP：用户输入 > 系统配置 > site_url 解析 > server.conf > 回退 127.0.0.1

- **权限控制问题**
  - 修复非 admin 用户（普通用户）登录后访问客户端和系统设置页面报错的问题
  - 后端中间件调整为：允许普通用户访问 `/ovpn/client` 所有方法和 `/ovpn/settings` GET 请求
  - 前端动态菜单根据用户角色显示，普通用户只显示概览、客户端、连接历史、个人设置 4 项
  - 前端路由守卫防止未授权访问，自动重定向到概览页

- **前端空值检查缺失**
  - 修复客户端页面访问 `settings.client.client_url` 时未做空值检查导致的运行时错误
  - 使用可选链操作符（`?.`）保护所有可能为 undefined 的对象属性访问

### Features

- **MFA 绑定后自动推送配置**
  - 用户绑定 MFA 成功后自动重新生成客户端配置文件（含 `static-challenge`）
  - 自动发送邮件通知，附件为新配置文件
  - 个人设置页面显示提示信息，引导用户重新下载配置文件

- **系统设置新增 VPN 服务器地址配置**
  - 系统设置 > 基础配置新增"VPN 服务器地址"输入框
  - 支持配置公网 IP 或域名，用于客户端配置生成
  - 配置优先级：用户创建客户端时输入 > 系统配置项 > 自动解析

- **客户端创建表单优化**
  - 移除"是否启用 MFA"开关，改为动态查询用户 MFA 状态
  - 描述文字调整为："MFA 验证将根据用户绑定状态自动启用"
  - 新增"服务器地址"输入框，支持覆盖系统默认配置

### Improvements

- **代码优化**
  - 新建客户端配置接口改用 Go 原生实现 `generateClientConfigGo`，减少对 shell 脚本的依赖
  - 客户端配置生成逻辑统一，避免生成和下载时判断不一致
  - 前端表单验证优化：字段级错误显示（红框+下方提示），后端错误使用 toast 通知

- **用户体验提升**
  - Toast 通知位置调整为顶部居中，主题自动跟随当前主题（日间/夜间/极光/翡翠）
  - 表单占位符文字调小调淡（12px, 28% 透明度），与标签（14px）形成层次
  - 所有表单统一左对齐标签布局：两列网格（标签固定 140px 宽度，右对齐文本）
  - 表格操作列按钮左对齐，与其他单元格对齐

### Security

- **权限隔离加固**
  - 后端接口增加细粒度权限控制，普通用户无法访问管理功能（用户管理、系统设置等）
  - 前端菜单动态渲染，避免暴露无权限功能的入口
  - 路由守卫防止通过 URL 直接访问未授权页面

## [v0.1.14] - 2026-07-26

### Bug Fixes

- **通知消息内容为空修复**
  - 修复发送成功时 NotifyLog 存入空字符串而非实际消息内容的问题
  - 确保站内信和邮件推送都包含完整的消息文本

## [v0.1.13] - 2026-07-26

### Bug Fixes

- **客户端生成和证书更新失败修复**
  - 修复客户端配置生成失败的问题
  - 修复证书更新失败的问题

## [v0.1.12] - 2026-07-25

### Features

- **客户端打包管理**
  - 支持批量下载客户端配置文件（打包为 ZIP）
  - 新增客户端管理增强功能

- **VPN 到期提醒**
  - 新增证书到期时间提醒功能
  - 支持邮件推送到期通知

- **通知系统完善**
  - 统一通知派发逻辑，确保所有事件都写入 NotifyLog
  - 前端实时推送通知，支持 WebSocket 订阅

- **SPA 路由 404 修复**
  - 后端 NoRoute 处理：API 路径返回 404 JSON，其他路径回退到 index.html
  - 支持 React Router v7 路由刷新不 404

## [v0.1.11] - 2026-07-24

### Bug Fixes

- **生产环境登录页白屏修复**
  - 移除 app.js/css 版本参数避免重复加载导致 React 双实例冲突
  - 修复 Vite 构建的 chunk 内部引用无参数的 ./app.js 导致浏览器加载两次

## [v0.1.10] - 2026-07-24

### Chore

- **BMAD-METHOD 框架整合**
  - 整合 BMAD-METHOD v6.10.0 AI 敏捷开发框架
  - 启用 claude-code/codex/trae 三套工具链
  - 配置中文环境与 expert 技能等级
  - `_bmad-output` 工件目录加入 .gitignore